<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme (Night) -->
    <style name="Base.Theme.StreamFlix" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/netflix_red</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowBackground">@color/black</item>
    </style>

    <style name="Theme.StreamFlix" parent="Base.Theme.StreamFlix" />

    <!-- Splash Screen Theme (Night) -->
    <style name="Theme.StreamFlix.Splash" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/streamflix_thumbnail</item>
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.StreamFlix</item>
    </style>
</resources>

